pytest==8.3.4
pytest-asyncio==0.24.0
pytest-mock==3.14.0
# anyio parametrises these tests over both backends; without trio installed
# the trio half is silently not collected, so CI covered less than local dev.
trio==0.27.0
